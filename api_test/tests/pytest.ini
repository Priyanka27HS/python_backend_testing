# create a section as pytest
[pytest]
# Register markers first
;markers =
;    smoke
;    regression
;    slow
;    fe

# filter warnings
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning

log_cli = true
log_cli_level = debug
